{
	instances = {
		begin = Dir() {
			default->Module(f"view.conf@begin", remap = { url = "directoryname" }) {
				view->Template(f"templates/aanpaslink.html") & Template(f"templates/article.html");
			};
			admin->Module(f"control.conf@byurl");
		};
	};
}