{
	instances = {
		credentials = CredentialsStore(
			credset = "wiki", host = "localhost", 
			db = "wiki", user = "wiki", pass = "wiki", 
			pool = true
		);

		begin = Template(f"templates/skelet.html") {
			content_branch = SiteSubsection() {
				default_branch = Module(f"view.conf@begin", remap = { url = "branchname" });
				admin_branch = GetFieldReader(field_url = "^[A-Z|a-z|0-9|_|-]+$") {
					form_branch = Template(f"templates/invalidurl.html");
					successful_branch = Module(f"view.conf@begin") {
						view_branch = Module(f"control.conf@begin");
					};
				};
			};
		};
	};
}