{
	instances = {
		credentials = Remember("wiki", 
			host = "localhost", db = "wiki", 
			user = "wiki", pass = "wiki", 
			pool = true, dbtype = "mysql"
		);

		begin = Module(f"login/auth.conf@enticate") {
			authenticated_branch = SiteSubsection() {
				default_branch = Module(f"view.conf@begin", remap = { url = "branchname" }) {
					view_branch = Template(f"templates/aanpaskader.html") {
						article_branch = Template(f"templates/article.html");
					};
				};
				admin_branch = Template(f"templates/loginwrapper.html") {
					article_branch = Module(f"control.conf@byurl");
				};
				logout_branch = Module(f"login/auth.conf@end");
			};
			unauthenticated_branch = SiteSubsection() {
				default_branch = Module(f"view.conf@begin", remap = { url = "branchname" });
				admin_branch = Template(f"templates/loginplease.html");
				login_branch = Module(f"login/auth.conf@identify");
			};
		};

		standalone = Template(f"templates/skelet.html") {
			content_branch = instances.begin;
		};
	};
}