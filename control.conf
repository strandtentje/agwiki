{
	instances = {
		save = MySqlQuerier("queries/SetBody.sql", params = [
			"article", "version", "title", "summary", "content"], defaults = {
			version = 1
			}, credset = "wiki"	
		) {
			successful_branch = Template("templates/savedarticle.html");
		};

		begin = PostFieldReader(
			escapehtml = false,
			field_title = "^.{1,512}$",
			field_summary = "^.{0,250}$",
			field_content = "^.*$"
		) {
			form_branch = Template("templates/editarticle.html");
			successful_branch = MySqlQuerier("queries/GetArticle.sql", params = [
				"url"], credset = "wiki"
			) {
				none_branch = MySqlQuerier("queries/SetArticle.sql", params = [
					"url"], credset = "wiki"
				) {
					single_branch = instances.save;
				};
				single_branch = instances.save;
			};
		};
	};
}