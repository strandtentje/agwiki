{
	instances = {
		begin = MySqlQuerier(f"queries/GetArticleBody.sql",
			params = ["url"], credset = "wiki", 
			defaults = { url = "main" }
		) {
			none_branch = Return("view") {
				default_branch = Template(f"templates/noarticle.html");
			};
			single_branch = Return("view") {
				default_branch = Template(f"templates/article.html");
			};
		};
	};
}