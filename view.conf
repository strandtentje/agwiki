{
	instances = {
		begin = DataReader(f"queries/GetArticleBody.sql", connection = "agwiki") {
			none->Return("view") {
				default->Template(f"templates/noarticle.html");
			};
			single->Return("view") {
				default->Template(f"templates/article.html");
			};
		};
	};
}